---
- name: Install Ruby & Bundler
  hosts: all
  become: true
  tasks:
  - name: Install ruby & bundler & build essential
    apt: "name={{ item }} state=present"
    with_items:
    - ruby-full
    - ruby-bundler
    - build-essential